<%= form.fields_for :avatar , :html => {:multipart => true} do |avatar_form| %>
				
	  <div class="field">
	    <%= avatar_form.label :photo, "Subir archivo" %>
	    <%= avatar_form.file_field :photo %>    
	  </div>
<!-- 
	  <% unless avatar_form.object.nil? || avatar_form.object.new_record? %>
	    <div class="field">
	      <%= avatar_form.label :_destroy, 'Remove:' %>
	      <%= avatar_form.check_box :_destroy %>
	    </div>
	  <% end %> -->


	<% end %>