<%= form_for([@resource, @resource.comments.build],  :method => :post ) do |f| %>
	<div class='field'>
		<!-- <textarea class='span12' placeholder='' rows='6'></textarea> -->
		<%= f.text_area :text, class: "span12", rows: '6' %>
	</div>
	<div class='actions'>
		<!-- <button class='btn btn-large'>Comentar</button> -->
		<%= f.submit "Comentar" ,  class: 'btn btn-large' %>
	</div>

<% end %>  